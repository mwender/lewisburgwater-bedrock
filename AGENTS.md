This directory contains `public/` which is a standard WordPress (WP) install. I would like to create a SKILL.md which converts this standard WP setup to a `roots/bedrock` framework setup.

As we write this skill, I want to generalize it such that it will work with any of my standard WP sites that I setup here locally on this machine.

Let's work together to perfect this process/skill such that it functions as automatically as possible anytime I want to run it on a new site.